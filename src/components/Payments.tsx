function Payments(params: {amount: number}) {
    return (<>
        <h2>Este es el monto a pagar papu: {params.amount}</h2>
        <p>Le puse pagos porque no me acordé de ningun otro módulo :V</p>
    </>)
}

export default Payments;